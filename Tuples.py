#tuples are similar to lists, but they cannot be changed.
# they are immutable

#list --> numbers = [1, 2, 3]
#tuple --> numbers = (1, 2, 3)

#you can't do the same methods that you can with lists.
#You can only get the index and count
numbers = (1, 2, 3)
print(numbers[0])